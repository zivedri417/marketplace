fix the reset password page
the web app is deployed, but when im trying to reset password, the email sent, and the link directs to a page that not found, a local host page, fix it and make sure the reset password page exists and works.
also make sure the verification email link works and directs to a valid confirmation page.
